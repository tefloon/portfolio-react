import React from "react";
import SectionHeader from "../elemenets/sectionHeader";

export default function Skills() {
  return (
    <section
      id="skills"
      className="flex flex-col items-center h-[600px] w-full scroll-mt-40"
    >
      <SectionHeader header="My Skills" />
    </section>
  );
}
